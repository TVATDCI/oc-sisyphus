# Volatile runtime state (operator working files)
node_modules/
.omo/
.codegraph/
.sisyphus/

# vs code workspace files
.vscode/*
.vscode/settings.json
# Toolchain: npm-ONLY at repo root (package-lock.json is canonical).
# bun.lock at root is gitignored — do NOT commit it. The Jun 21 decision
# is npm-only (oh-my-openagent installed via `npm install`, NOT `bun add`).
# (The website-analyzer skill has its own bun subproject — that NESTED
# bun.lock is NOT matched by this root-anchored `/bun.lock` rule.)
/bun.lock

# Backup dirs
*.backup*
*.backup-*
COMPLETE-CODEBASE.md.backup
oh-my-openagent.json.backup
opencode.json.full

# Logs
*.log

# OS files
.DS_Store
*.tmp
*.temp

# Build artifacts (only ignore root build dirs, not plugin dist/)
build/
.next/
coverage/

# Auto-generated task tracking (OpenCode internal)
tasks/
lsp-install-decisions.json

# Archived session state (historical snapshots)
archive/
*.bak.*
