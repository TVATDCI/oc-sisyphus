# OpenCode Codebase — Complete Structure, Changes & Workflow

> **Last reviewed:** 2026-08-15 | **Next review:** When agent routing changes or a new skill is added
> **Drift-prone sections:** §Agent Routing, §Change Timeline, §Skill count, §Plugin file count
> **Stable sections:** §Directory Architecture, §Workflow State Machine, §Subagent Permissions
> **Deep history + rationale (searchable):** Main-vault `wiki/concepts/` system-history pages (tags: `system-history`, `era-1`..`era-4`). Era pages WikiLink to event pages and the opencode-architecture concept pages.

> **🔧 TOOLCHAIN (canonical — do NOT deviate):** npm-ONLY at repo root. `package-lock.json` is the source of truth; **no `bun.lock`** (gitignored at `/bun.lock`). Install/upgrade oh-my-openagent via `npm install oh-my-openagent@<ver>`, **never `bun add`** (a Jul 17 bun-add runbook regressed the Jun 21 npm-only decision and created a stale-lockfile drift; reverted). Decision rationale: all repo scripts (`pre-push.sh`, `package.json`) are npm-based; the plugin's committed lockfile is `package-lock.json`. The `skills/website-analyzer` bun.lock is a separate skill-local subproject — not covered by this rule.

1. Directory Architecture (7 layers)
   ~/.config/opencode/
   ├── 1. ORCHESTRATION LAYER
   │ ├── opencode.json # Entry-point config
    │ └── (agent routing now in ~/.omo/omo.jsonc — [opencode] section; legacy oh-my-openagent.json.bak)
   │
   ├── 2. INSTRUCTION LAYER
   │ ├── AGENTS.md # Root system prompt (compaction, routing)
   │ ├── SYSTEM-OVERVIEW.md # Architecture doc v4.1
   │ ├── rules/ # 14 rule files (4 language + 9 concern + README)
   │ ├── prompts/ # Context injection (main-vault, prometheus)
   │ ├── agents/ # 8 subagent .md definitions with permissions
   │ └── commands/ # Slash-command definitions (reflection)
   │
   ├── 3. SKILL LAYER
    │ ├── skills/ # 47 real skill directories + 1 \_shared refs (48 total)
   │ ├── each has SKILL.md + optional scripts/evals
   │ └── Notable eval sets: code-review, git-commit-message, skill-creator; operator-owned fan-out review: orchestrator-review (fork of plugin-bundled review-work; Sisyphus gate lanes — code-review + security-auditor + ui-auditor + goal-verify + conditional regression-gate + oracle second-opinion)
   │
   ├── 4. PLANNING LAYER
   │ ├── ~/.sisyphus/ # Canonical state machine (consolidated from .sisyphus/): state.json, hotcache, evidence, notepads, plans, archive
   │ └── .omo/ # ARCHIVED (W0.3): retained as read-only historical reference; sole live state is ~/.sisyphus/
   │ └── Log split (4-layer, enforced by skill:session-close): Layer 1 brief → SYSTEM-NARRATIVE.md; Layer 2 detail → ~/Main-vault/log.md (vault repo). See `skill:session-close` for routing.
   │
   ├── 5. TRACKING LAYER
   │ └── tasks/ # Beads per project (one subdir per active project)
   │
   ├── 6. INFRASTRUCTURE LAYER
   │ ├── scripts/ # 18 scripts (load-rules, regression-gate, validate-skills-v2.py, verify-plugin-compat.js, check-doc-claims.sh, check-completion-honesty.sh, bd_remember.py, mcp-home-wrapper.sh, omo-query.js, etc.)
   │ ├── plugins/sisyphus-gates/ # Governance gate plugin: 19 src modules + cli.js + dist/index.js + 25 test files (648 tests). HMAC-SHA256 verdict signing, trust-root path protection (incl. opencode.json write-protect + READ_EXCEPTION_PATTERNS), MCP classification, Layer 3.7 sandbox allowlist (path-scoped command relaxation for /tmp/), Layer 6.5 session-close gate (blocks git push / bd dolt push when session_close.status === "open"; protocol start/complete/override CLI), hasShellMetachar metachar denial, throw enforcement, adversarial test suite, stripLeadingEnvExport + SUBCOMMAND_BD classifier
   │ ├── benchmark/ # 3 JSONL baseline runs (vs codegraph vs semble)
   │ └── .codegraph/codegraph.db # Search/index: semantic code index SQLite DB
   │
   └── 7. UI LAYER
   ├── tui.json # Terminal UI config (plugin: [])
   └── .vscode/settings.json # VS Code workspace settings

   ── MCP filesystem scoping (design property, not a bug) ──────────────────
   The `home` MCP server (`@modelcontextprotocol/server-filesystem`) starts
   with the 6 paths listed in `mcp.home.command`, but at runtime the MCP
   **Roots protocol** overrides them: opencode advertises ONLY the current
   workspace root as the MCP root, and the server replaces its allowed
   directories with that single root. So:
   - `cd ~/.config/opencode && opencode` → MCP sees only `~/.config/opencode`
   - `cd ~/Main-vault && opencode` → MCP sees only `~/Main-vault`
     The 6 paths in `mcp.home.command` are startup args; runtime scoping is
     workspace-root-bound. This is inherited from Claude Code's security model.
     Cross-workspace file access does NOT go through MCP `home_*` tools — it
     goes through **agent-level permissions** (e.g., `archivist` has explicit
     `edit` allow rules for `~/Main-vault/log.md` regardless of workspace).
     `permission.external_directory` (opencode.json) controls built-in file
     tools only, not MCP roots; setting it does not change MCP scoping.

2. What Changed — Full Timeline
   Date Event Artifact
   May 4 Security-auditor skill designed security-auditor-design-history.md
   May 8 v2.1.1 — AGENTS extraction plan extract-agents-best-practices.md created
   May 8 12 rule files adapted with provenance rules/{languages,concerns}/\*.md
   May 8 Smoke test PASS (img-upload-with-multer) smoke-test-v2.1.1.md
   May 9 v2.1.2 — Hard gates implemented session-handoff-v2.1.2-.md
   May 24 .omo/notepads copy updated Duplicated from .sisyphus
   May 29 Compaction + memory injection features hermes-pattern-study.md, AGENTS-context-test-plan.md
   May 29 AI tools 2nd-pass evaluation good-ai-list-rescan-2026.md
   May 30 Website-analyzer v1.5.0 test ✅ PASS website-analyzer-v1.5.0-test.md
   May 30 SPA hydration negative test ✅ PASS spa-hydration-negative-test.md
   May 30 Source content refs test ✅ PASS source-content-ref-test.md
   May 30 Session close + migration sync session-close-2026-05-30.md
   May 31 Plan remediation session (THIS session):
   Oracle review: 4 of 9 criteria FAIL/WRONG Found criteria 4/5/7 were already done, 2 was broken
   scripts/load-rules.sh created Automated rule detection + loading (replaced manual Step 0)
   scripts/regression-gate.sh created Workflow validation gate
   SYSTEM-OVERVIEW.md updated v2.1.1 history added, orchestration category removed
   agents/athena.md fixed git-commit-message → athena-research skill
   agents/post-reviewer.md fixed Missing subagent frontmatter
   oh-my-openagent.json fixed z-glm-5.1 → glm-5.1 typo
   rules/frameworks/ removed Empty directory cleanup
   scripts/validate-skills.sh fixed \_shared skip logic
   VCS evidence filed vcs-integration-2026-05-31.md
   Plan status updated "Complete after remediation (2026-05-31)"
   Session handoff session-handoff-2026-05-31.md
   Jun 1 Oracle sisyphus/oracle flip (THIS session):
   Sisyphus primary → minimax-m3 (was kimi-k2.6); ultrawork → qwen3.7-max
   Oracle primary → qwen3.7-max (was glm-5.1); fallbacks [glm-5.1, minimax-m3]
   tui.json plugin fix Added "oh-my-openagent/tui" to plugin array (fixed doctor warning)
   Jun 20 oh-my-openagent.json reset + doc sync: provider/model prefixes restored in §Agent Routing; MCP home path aligned to ~/Main-vault; tui.json plugin array cleared
   Jun 24 Provider switch: full zhipuai-coding-plan reset (14 of 17 agent primaries on zai, 5 of 9 categories). Backup at oh-my-openagent.json.backup (mixed opencode-go + opencode scheme). §Agent Routing updated; includes coverage of sisyphus-gates classifier fixes (stripLeadingEnvExport + SUBCOMMAND*BD) landed same day.
   Jun 25 Provider strategy v3 (3-tier fallback): zhipuai-coding-plan subscription (Tier 1, primary) → opencode-go lite subscription (Tier 2) → opencode pre-pay-as-you-go (Tier 3, free + paid). Trigger: prior mixed-provider scheme failed because OpenCode does not honor zai as cross-provider fallback target — forced opencode zen (paid extra) instead. 13 of 17 agents + 6 of 9 categories on zai primary; prometheus moved to opencode-go/kimi-k2.7-code; ultrabrain→opencode-go/kimi-k2.7-code, artistry→opencode-go/mimo-v2-omni, quick→zai/glm-4.5-air, unspecified-low→zai/glm-4.6, unspecified-high→zai/glm-4.7. §Agent Routing rewritten with 3-tier strategy rationale + fallback chain coverage gaps (atlas zai-only, archivist/athena no fallbacks, explore/auditor/explorer inverted). Concurrency hardening (same day, post-librarian research): modelConcurrency gap fixed (added glm-4.6v=2, glm-4.6=5, glm-5-turbo=3 — previously shared single 5-slot zai bucket via providerConcurrency fallthrough); restore_primary_after_cooldown=true added to runtime_fallback so post-fallback the runtime returns to primary zai after cooldown_seconds (15s); §Agent Routing documents queuing≠fallback distinction (concurrency cap queues task, only HTTP errors like 429/5xx trigger runtime_fallback to next tier).
   Jun 25 teach skill added: Markdown-first, multi-session learning workspace under ~/Main-vault/teach/<topic>/. Adapted from Matt Pocock's productivity/teach skill; drops HTML/assets pipeline, switches to Markdown lessons + reference docs + learning records. F3 validation cycle produced 4 behavioral fix commits (abort path, workspace resolution, front-loaded FIRST ACTION blockquote, worked example + single-workspace default). Test workspace at ~/Main-vault/teach/optimize-opencode-system/ has MISSION.md, RESOURCES.md, GLOSSARY.md, 4 lessons, 1 reference, 2 learning records.
   Jun 26 teach clarification reflex fixed: root cause was sisyphus agent intent gate (oh-my-openagent.json:19 "If ambiguous, ask the user") firing at system-prompt level before skill body was read — explaining why 4 rounds of skill-body edits had no effect. Fix: moved workspace-resolution directive from skill body to skill description (frontmatter line 3), which is read during skill matching (concurrent with intent classification). F3 passed: zero clarification questions on ambiguous topic phrase "teach me how to optimize the current system". Transferable insight: for skills that need to override default agent behavior, put the override in the description, not the body.
   Jun 26 Response & Gate Discipline — gate/refusal/memory/search discipline absorbed from frontier prompts (AGENTS.md new section between Doc Drift Guard and Shell Safety; sisyphus.prompt_append extended with response discipline; RAW.md created as research reference; COMPLETE-CODEBASE.md drift note added).
   Jun 26 Prompts archive: moved all inline prompt_append values to file:// references under prompts/*.md (sisyphus, hephaestus, oracle, librarian, explore agents + git-commit-message category). prompts/ is now the canonical external-prompt archive; oh-my-openagent.json stays concise. prometheus was already file:// (unchanged). README repo layout updated.
   Jun 26 Agent/category model reset (operator-driven): ultrabrain category moved from opencode-go/kimi-k2.7-code to zhipuai-coding-plan/glm-5.2; prometheus moved from opencode-go/kimi-k2.7-code to zhipuai-coding-plan/glm-5.1; explore moved from opencode/deepseek-v4-flash-free to opencode-go/minimax-m2.7; atlas moved from glm-5.1 to glm-5-turbo; auditor and explorer moved from opencode/deepseek-v4-flash-free to zhipuai-coding-plan/glm-4.7. Trigger: opencode-go subscript usage very low, consolidating to zhipuai-coding-plan primary. Net: zai 13→16 agent primaries, opencode 3→0, opencode-go categories 2→1. §Agent Routing updated.
   Jun 7 W0 — Safety baseline: git init, .gitignore fix, .omo/ archive, plugin pin @4.7.5, benchmark/ removal
   Jun 7 W1 — Runtime hardening: MCP scoped to 4 paths, health-check.md, README.md, validate-skills-v2.py (37 PASS), 6 new agents added
   Jun 7 W2 — Contract migration: canonical state seeded (~/.sisyphus/state.json), plugin source restored (13 modules), 180 unit tests + 20 self-tests PASS, THREAT-MODEL.md
   Jun 7 W3 — Automation: pre-push hook, GitHub Actions CI, doc drift guards (check-doc-claims.sh, check-workflow-contract.sh), full doc rewrite SYSTEM-OVERVIEW v4.0, private remote push
   Jun 7 Fix All minimax-m3-free replaced with deepseek-v4-flash-free (12 replacements); patch-package removed (not applicable to cache-based runtime)
   Jun 8 W4 — Synthesis project: fullstack-dev skill created (863 lines, 48/48 evals) + subagent; frontend-ui-ux enhanced (anti-slop, motion, copywriting); code-review v1.1.0 (architecture patterns); security-auditor v1.3.0 (CORS/rate-limit/shutdown); document-builder created (PPTX via PptxGenJS); website-analyzer v1.6.0 (UI Critique Mode)
   Jun 8 W4 — shader-dev: constrained WebGL2 fragment-shader skill (oracle architecture, 6 recipes, 6/6 evals, 3 bugs fixed)
   Jun 8 Env Canonical path consolidation: .sisyphus/ → ~/.sisyphus/; HANDOFF.md/hotcache.md deleted; CLEANUP.md created; stale iteration-1 evals pruned
   Jun 8 Git 10 atomic commits across all 7 skills; doc-claims drift fixed (subagents 7→8, directories 38→43)
   Jun 17 Absorption from remote opencode-config repo: intent gate added to sisyphus agent prompt*append; execution-receipt and reflection skills created; check-completion-honesty.sh topology gate added; COMPLETE-CODEBASE.md counts reconciled (skills 43→45, scripts 14→15)
   Jun 21 oh-my-openagent upgraded 4.7.5 → 4.12.1: opencode.json/plugin pin, package.json/package-lock.json, tui.json plugin entry, removed stale bun.lock; .codegraph/codegraph.db untracked (already gitignored); doctor clean except pre-existing mimo-v2.5-pro fallback
   Jun 24 Absorption from remote opencode-config (TNTHNGVDYNND): Phase 0-6 security hardening + HMAC signing pivot. Oracle-consulted strategy (ses_106ac03b3ffe4Jc2iKnLVqiDNF). 6 phases: (1) adversarial tests ported, (2) behavioral hardening — throw enforcement, Layer 0 trust-root paths, Layer 3.5 MCP classification, task fail-closed, command-policy chaining/substitution/wrappers, (3) HMAC-SHA256 signing core — verdict-signing.js, memory-key.js, gate-logger.js, state.js pivot to signed verdicts, (4) CLI signing tool — cli.js with sign-verdict + approve commands, (5) workflow.yaml update — stale path fix, W1.B transition fix, forgeable auto-advance removed, schema 2.0.0→3.0.0, (6) doc update. Plugin src 13→18, tests 9→15 (180→322 tests), new: trust-root-paths.js, mcp-classifier.js, verdict-signing.js, memory-key.js, gate-logger.js, cli.js. opencode.json plugin config changed to array form with verdict_key_command. HMAC key at ~/.local/share/sisyphus-gate-key (chmod 600). Hard cutover: unsigned verdicts no longer trusted
   Jun 27 Layer 3.7 sandbox allowlist shipped: path-scoped command relaxation for /tmp/ directories. 7 slices (A–G) via full Sisyphus ceremony. New: src/sandbox-policy.js. Modified: gates.js (Layer 3.7 insertion), command-policy.js (hasShellMetachar extraction + \_internal export), plugin.js (sandbox-allow audit), state.js (sandboxConfig field), trust-root-paths.js (opencode.json protect + READ_EXCEPTION_PATTERNS), metrics.js (event_subtype override). Tests: 328→431 unit (+103), 20→22 e2e (+2). opencode.json sandbox config enabled. Known gap: READ_EXCEPTION_PATTERNS read-tool runtime (low severity, tracked).
   Jun 28 READ_EXCEPTION_PATTERNS gap closed: 3-line fix in gates.js routes read tools through matchTrustRootRead; AC-3.17 passes at runtime (the known low-severity gap from Jun 27 ship is resolved). Same day: SYSTEM-NARRATIVE.md created as 3-layer documentation bridge (351 lines) — makes the deep developmental archive at ~/developer/Reference/meta/ (170KB, 16 files, was invisible to agents) reachable from AGENTS.md (1-line reference added in On-Demand Reference section) + system-reference skill (Full System Report capability added, description extended with system-history/report triggers + SYSTEM-NARRATIVE.md load directive). Narrative structured by ERA (Foundation Apr 30–May 7 / Growth May 7–22 / Hardening May 22–Jun 24 / Production Jun 24–present) with cross-references to primary sources; living "Current System State" section for session-close updates.
   Jun 29 System cleanup + skill quality mapping + vault migration planned. Cleanup: stale backups (COMPLETE-CODEBASE.md.backup, oh-my-openagent.json.backup) + raw eval outputs (fullstack-dev-workspace/iteration-2/eval-*) deleted, evidence/notepads pruned (May files removed), pushed to remote. Skill quality: mapped all 50+ skills, identified collision zones (Review 7, Research 4, Plan 5, Security 3, Frontend 4), approach corrected after operator feedback (pipeline skills ≠ user-invoked skills; splits were deliberate and tested — momus-split-test report from May 30 proves this). Reflection analysis: 6 findings (understand-before-proposing HIGH, pipeline-vs-user-invoked HIGH, respect-evidence HIGH, depth-over-speed MEDIUM, document-for-continuity MEDIUM, resource-constraints LOW). Read full meta/ developmental archive (170KB, 16 files: ARCHIVE-DEVELOPMENTAL-DOCUMENT 542 lines, pi-vs-sisyphus, sisyphus-pipeline-improvement, HYBRID-PLAN 1033 lines, DOCUMENTATION*INDEX, templates). Vault migration plan created by Prometheus at .omo/plans/main-vault-system-history-timeline.md (308 lines, 15 todos, 4 waves, 8 WikiLinked pages for Main-vault wiki/concepts/). Next: execute vault migration (MCP prerequisite: add ~/developer/Reference/meta + ~/developer/test-artifacts to opencode.json mcp.home.command), then continue skill quality optimization Zone 1 (Review skills).
   Jun 29 Session-log architecture refactored to 4-layer routing: SYSTEM-NARRATIVE.md L320 block (≤3 lines/session, hard cap) → ~/Main-vault/log.md (verbose, via archivist delegation) → .omo/evidence/session-close-{date}-*.md (raw) → bd remember (memory). Root cause: session-close/SKILL.md never mentioned SYSTEM-NARRATIVE.md, so agents improvised and dumped verbose content inline. Fix: skill now enforces the routing explicitly. Duplicate verbose block (former SN L352-365) migrated to Main-vault/log.md as Layer 2 entries.
   Jun 29 MCP filesystem scoping investigated + documented. Symptom: `home_list_allowed_directories` returned only the workspace root despite `mcp.home.command` listing 6 paths and `pgrep` confirming server-filesystem process running with 6 path args. Root cause: MCP Roots protocol — opencode advertises only the workspace root as MCP root; server-filesystem then replaces its startup allowed directories with the client's roots response. `permission.external_directory` (string and object forms both tested) does not map to MCP roots. Conclusion: this is by design (inherited Claude Code security model), not a bug. `permission` section removed from opencode.json as non-functional. Cross-workspace access stays via archivist agent delegation (explicit edit permissions). MCP scoping property documented in §1 Directory Architecture above. Oracle consulted (ses*0ec2bd0e3ffe70q1QxBFi7jOBX, ses_0ec87e622ffeXvr0GpQUhGLg0G) — both routed to opencode-go/kimi-k2.7-code via fallback (Oracle routing issue, separate investigation).
   Jun 29 Oracle routing investigation + fix. Root cause: `zhipuai-coding-plan/glm-5.2` had `modelConcurrency: 1`, shared across 6 consumers (sisyphus, oracle, momus, post-reviewer, reviewer + ultrabrain category). When sisyphus held the slot (any active session), Oracle calls fell through to fallback #1 (`opencode-go/kimi-k2.7-code`). Task metadata "category: unknown" was metadata labeling, not a routing bug. Fix in oh-my-openagent.json: (1) `glm-5.2` concurrency 1→2 (Oracle can now run alongside sisyphus), (2) `glm-5.1` 2→3, (3) `glm-5v-turbo` 2→1 (now the most-constrained model — visual-engineering), (4) zai `providerConcurrency` 5→10 (matches opencode-go/opencode), (5) new `background_task.{defaultConcurrency:10, staleTimeoutMs:180000}` fields. Fallback chain updates: oracle removed `qwen3.7-max` (now [kimi-k2.7-code, gpt-5.4]); post-reviewer + reviewer switched to `[opencode-go/glm-5.2, opencode/deepseek-v4-flash-free]` (preserves 5.2 quality on fallback); artistry second fallback switched to `opencode/gemini-3-flash`. Primary models unchanged for all 17 agents.
   Jun 29 oh-my-openagent upgraded 4.12.1 → 4.14.0: Oracle-reviewed (ses_0eb7d514dffeZqfHPAwH09VUkc), all 8 load-bearing surfaces COMPATIBLE. Schema forward-compatible (4.14.0 adds restore_primary_after_cooldown which 4.12.1 omitted). 4.13.0 (previously deferred) feature delta: TeamMode v2, Ultimate Browsing skill, AST-grep MCP → sg resolver, provider-exhaustion fallback for background tasks. 4.14.0: unified telemetry, Coding Agent Sessions skill, Atlas verdict classification, ulw-research rename (ultraresearch alias). Config changes: auto_update: false added to oh-my-openagent.json (prevents drift); opencode.json pin bumped 4.12.1→4.14.0. Installed via bun add oh-my-openagent@4.14.0 (postinstall trusted via bun pm trust). Deps unchanged from 4.12.1. Backups at opencode.json.bak.4.12.1 + oh-my-openagent.json.bak.4.12.1.
   Jun 30 Agent-config gaps follow-up (.omo/drafts/agents-gaps-followup.md, 5 tasks): (1) explore/explorer tier distinction documented via YAML comment in agents/explorer.md only — initial attempt to add `_comment` field to oh-my-openagent.json `explore`/`explorer` entries was schema-rejected (agent-entry schema is `additionalProperties:false`; `_comment` undeclared; Oracle-confirmed ses_0e6772a69ffeaH1cWEfBkx5FWg); JSON annotation abandoned; no rename (operator-confirmed intentional split: explore=opencode-go/minimax-m2.7 cheap grep, explorer=zhipuai-coding-plan/glm-4.7 capable research). (2) Permission audit across all 8 user-defined .md files: clean on all operator-stated criteria (modern permission: block, mode: subagent, load_skills where needed, -f bash flags); minor stylistic variance noted — auditor/post-reviewer/reviewer use `edit: deny` shorthand vs object form in other 5 (both valid via migrateAgentConfig, documented not normalized). (3) §Subagent Permissions rewritten: 18-agent total = 8 user-defined (oracle, archivist, athena, auditor, explorer, reviewer, post-reviewer, fullstack-dev-tester) + 10 plugin-bundled (sisyphus, hephaestus, librarian, explore, multimodal-looker, prometheus, metis, momus, atlas, sisyphus-junior — sourced from plugin install dir, no .md in agents/). (4) fullstack-dev-tester `edit: *: allow`documented as intentional (different blast radius than archivist's scoped vault paths; agent's job is to BUILD apps anywhere). (5) README §5 drift fixed ("17 Specialized Personas" → 18 + plugin-bundled/user-defined split added). Residual`zai`tokens cleaned: README L108 (1) + COMPLETE-CODEBASE §Agent Routing L196 prose (4 standalone`zai`→`zhipuai-coding-plan`); historical timeline entries (Jun 24/Jun 26) intentionally retained as accurate state-at-time records. Auditor `temperature: 0.0`documented as deliberate deterministic-validation exception (other 7 at 0.1). Operator integrity audit mid-session caught false "Session fully closed" claim from prior instance — full 4-layer close executed this entry. Files: README.md, COMPLETE-CODEBASE.md, SYSTEM-NARRATIVE.md, agents/explorer.md, oh-my-openagent.json, .omo/evidence/session-close-2026-06-30-agents-gaps-followup.md.
Jun 30 Session-close gate MVP (Phase 1 + 1.5) — anti-drift mechanism blocks`git push`/`bd dolt push`when`session_close.status === "open"`. Direct response to Jun 29 "false Session fully closed" claim (prose non-authoritative; state field authoritative). Phase 1: gates.js `shouldBlockCommand`extended, cli.js cmdProtocol function (start/complete/override actions, deep-merge preserves started_at/completed_at audit trail across transitions, no HMAC — mechanical state update), skills/session-close/SKILL.md protocol lifecycle preamble + step 6 (complete before push), 8 unit tests in test/session-close-gate.test.js. Phase 1.5 (activation, same-day after Phase 1 dormant ship caught): cli.js`case "protocol":`dispatch wired + --reason flag in parseArgs, 6 subprocess regression tests in test/session-close-cli-subprocess.test.js that spawn`node cli.js protocol ...`and verify state transitions — catches the dispatch-gap class of bug forever (would have failed pre-Phase-1.5). Tests 431→446 (+15). 4 deviations from original draft: (1) return shape`{blocked, reason}`not draft's`{allowed, reason, layer}`— draft was wrong, actual API proven by fail-closed.test.js:296-322; (2) added`protocol start`command — draft GAP, without it the gate is dead code (status never reaches "open"); (3) deep-merge for session_close — writePersistentState's shallow`...extra`would erase started_at/layers; (4) step 6 placement before push not step 10 after — draft's step 10 was a deadlock (gate blocks push before complete runs). Trust model: protocol CLI runs in operator terminal (outside gate's tool boundary); session_close field inherits Layer 0 protection automatically (it's IN state.json). Evidence:`~/.sisyphus/evidence/session-close-2026-06-30-session-close-gate-mvp.md`. (Process notes: agent cannot git-add plugin source — operator ran all git mutations; multi-line `git add \` backslash continuation broke in zsh, caused a partial commit that was caught and amended; `cat -n` paste led to indent miscount that caused patch v1 to fail silently; `.omo/` is gitignored so drafts there are local-only.)
   Jul 1 Provider auth rename: `zhipuai-coding-plan` → `zai-coding-plan` (operator switched the provider login). oh-my-openagent.json already migrated by operator — all `zhipuai-coding-plan` references replaced with `zai-coding-plan` (17 agent primaries + 7 category primaries + providerConcurrency/modelConcurrency keys); same underlying GLM models, no routing/behavior change. Current-state doc references updated to `zai-coding-plan`: README §fallback strategy (L126), agents/explorer.md tier note (L2), SYSTEM-NARRATIVE Provider Strategy v3 (L224, L227) + Current System State (L323), COMPLETE-CODEBASE §Agent Routing (routing table L196, categories L197, Provider mix L198, explorer tier note L201). Historical timeline entries (Jun 24–30) intentionally retained as accurate state-at-time records per established convention (see Jun 30 entry). Note: standalone `zai` shorthand tokens in the Provider mix line + the phrase "Most constrained zhipuai model" left as-is (out of scope for the `zhipuai-coding-plan` → `zai-coding-plan` rename).
   Jul 17 Agent/category model refresh + concurrency hardening (operator-driven, multi-pass). 12 of 18 agent primaries + 5 of 9 category primaries changed. Provider mix shifted zai-coding-plan 16→14 agent primaries, opencode-go 1→3 (oracle→kimi-k3, multimodal-looker→mimo-v2-omni, explorer→minimax-m2.7), opencode 0→1 (explore→deepseek-v4-flash-free). Notable: sisyphus ultrawork glm-5.1→glm-5-turbo; hephaestus/atlas/archivist glm-5.1→glm-4.7; momus/post-reviewer/reviewer moved off glm-5.2 (Jun 29 contention fully resolved — glm-5.2 now primary for sisyphus only; oracle moved to opencode-go/kimi-k3); auditor glm-4.7→glm-5.1; explorer glm-4.7→opencode-go/minimax-m2.7 (final, after iterating through glm-4.4-air and opencode/deepseek-v4-flash-free mid-session — settled on opencode-go tier to restore the explore/explorer model-tier distinction); visual-engineering dropped its vision-specialized primary (glm-5v-turbo removed) for glm-5-turbo + vision fallbacks (gemini-3.1-pro); artistry moved mimo-v2-omni→glm-5.1. oracle fallback rewritten from [opencode/gpt-5.4] to [zai-coding-plan/glm-5.2, opencode-go/kimi-k2.7-code] (gpt-5.4 no longer referenced anywhere). glm-5.1 hotspot addressed: concurrency 3→4 (8 consumers on 4 slots). modelConcurrency map made COMPREHENSIVE: 10→22 entries spanning all 3 providers (5 zai + 7 opencode-go + 10 opencode) — nearly every referenced model now has an explicit per-model cap. Removed glm-5v-turbo/glm-4.6v/glm-4.6 (no longer referenced) and opencode-go/kimi-k2.5 (only opencode/kimi-k2.5 is used now). Added opencode-go/kimi-k3=1, opencode-go/qwen3.6-plus=1, opencode-go/glm-5.1=3, opencode-go/minimax-m2.7=5, opencode-go/mimo-v2-omni=1, opencode/deepseek-v4-flash-free=5 (free-tier cap), opencode/mimo-v2.5-free=3, opencode/gemini-3.1-pro=1, opencode/gemini-3-flash=1, opencode/hy3-free=3, opencode/glm-5.2=2, opencode/glm-5.1=3, opencode/kimi-k2.5=3, opencode/kimi-k2.6=2, opencode/kimi-k2.7-code=2. kimi-k2.7-code raised 1→2 (7 fallback positions); sisyphus dropped kimi-k3 from fallback chain (oracle is now the SOLE kimi-k3 consumer — contention fully resolved). Coverage gaps: ALL three Jun 25 gaps (atlas, archivist, athena) CLOSED — every agent has fallback_models. Earlier-session open flags all RESOLVED: deepseek-v4-flash-free capped at 5; sisyphus no longer competes for kimi-k3; kimi-k2.7-code at 2. Two cosmetic nits remain (both fall through to providerConcurrency=10, harmless): opencode/kimi-k2.6 dead entry (unreferenced), opencode-go/glm-5.2 missing explicit entry. §Agent Routing (L199-201) + tier note rewritten; SYSTEM-NARRATIVE Provider Strategy v3 + Current System State + session log + agents/explorer.md tier note synced.
   Jul 17 oh-my-openagent upgraded 4.14.0 → 4.18.2: no breaking changes in the 4.14.0→4.18.2 range touch our gate surfaces (Layer 6.5 cli.js protocol + Layer 3.7 sandbox + tuple-form plugin config all hook the stable tool pipeline); deliberately stopped short of 4.19.0 (refactor!: drop shared/ skill aliases would break skills/\_shared/). 4.18.2 fixes the category-skill-reminder tool-output corruption seen this session + plugin discovery respects XDG_CONFIG_HOME. opencode.json + tui.json pins bumped; installed via `npm install oh-my-openagent@4.18.2` (NOT `bun add` — an initial bun-add runbook regressed the Jun 21 npm-only toolchain decision and created a root bun.lock + stale package-lock drift; reverted: `rm -rf node_modules bun.lock && npm install`). Verified: 446 unit + 22 self-test PASS, verify-plugin-compat PASS (0 fail/0 warn), check-completion-honesty PASS. Watch-item post-restart: delegate-task contract changes (4.14.1/4.18.2) — confirm with a smoke-test delegation.
   Jul 18 Post-Jul-17 cleanup: config drift fixes + skill/model decoupling. oh-my-openagent.json: removed dead `opencode-go/kimi-k3: 1` from modelConcurrency (no consumer after oracle→kimi-k2.7-code in 93e9c64); restored `opencode-go/kimi-k2.6: 2` + `opencode/kimi-k2.6: 2` caps (silently removed in 93e9c64 but still referenced by atlas/archivist/auditor fallback chains — without the cap they fell through to providerConcurrency=10, risking rate-limit overrun). Docs synced to current config: SYSTEM-NARRATIVE Provider Strategy v3 + Jul 17 timeline (correction note appended), COMPLETE-CODEBASE §Agent Routing L202+L204 (oracle primary kimi-k3→kimi-k2.7-code). Skills: stripped hardcoded model identifiers from `**Category:** X → Y` lines in 12 SKILL.md files (issue-creator, wave-executor, plan-writer, website-analyzer, plan-closer, plan-updater, brief-loader, tdd-executor, prd-writer, workflow-guard, regression-gate, reference-checker); per Oracle review ses_088f46bf0ffejvUrXRRBYRft4x, skill-body model mentions have zero routing influence — runtime resolves from oh-my-openagent.json by category. Validator (scripts/validate-skills.sh): added FAIL-level regex guard `^\*\*Category:\*\*.*→` — caught a 12th drift case (regression-gate) on first run that manual grep had missed. Frontmatter fix: added missing `compatibility: opencode` to frontend-ui-ux (pre-existing FAIL). Validator result: 43 PASS / 10 WARN / 0 FAIL (was 41/10/2). Evidence: `~/.sisyphus/evidence/session-close-2026-07-18-post-jul-17-cleanup.md`.
   Jul 19 Skill-length cleanup (A+ plan executed) + validator Length Exception mechanism. Tier 1: deleted 189 lines of changelog bloat from website-analyzer/SKILL.md (v1.2.0/v1.2.1/v1.5.0 release notes + Day 1-5 checklists; 1496→1307). Tier 2: extracted website-analyzer Detection Rules Reference → `references/detection-rules.md` (1307→1129, 10 detection patterns with regexes + JSON report formats) and wave-executor Checkpoint Protocol → `references/checkpoint-protocol.md` (781→736, types/format/when-not-to-use/auto-mode), one-line pointers left inline. Tier 3: added `## Length Exception` section to 9 over-500 SKILL.md files (website-analyzer, wave-executor, document-builder, security-auditor, skill-creator, fullstack-dev, ui-auditor, agent-development, discovery-orchestrator) documenting why each is genuinely over-length and what pruning was already done. Option B (validator mechanism): patched `scripts/validate-skills.sh` line-count check to recognize `^## Length Exception\s*$` anchored section header — when present, emits `PASS (documented exception: ...)` instead of WARN; anchored match prevents prose mentions from accidentally suppressing real WARNs. Skill-creator L265 updated to document the concrete protocol (was vague "in the skill's notes"). Validator result: 43→52 PASS / 10→1 WARN / 0 FAIL (remaining WARN is `skills/fullstack-dev-workspace/README.md`, separate evaluation workspace).
   Jul 19 Agent routing rebalance (operator-driven; oh-my-openagent.json modified in working tree — separate commit). 3 agent primaries moved zai-coding-plan → opencode-go: oracle (kimi-k2.7-code → kimi-k3, returning to the model Jul 17 had abandoned as 'unreliable'; opencode-go/kimi-k3 modelConcurrency entry restored at cap 2, having been removed Jul 18 as dead), metis (zai/glm-5.1 → opencode-go/glm-5.1, cross-provider swap same model), reviewer (zai/glm-5.1 → opencode-go/kimi-k2.7-code, primary/fallback swapped with zai). Net provider tally: zai 14→11 agents, opencode-go 3→6, opencode 1. Hotspot shift: zai glm-5.1 primary consumers 5→3 (momus, auditor, fullstack-dev-tester remain); opencode-go glm-5.1 primary consumers 1→2 (prometheus + metis); opencode-go kimi-k2.7-code gained reviewer as 2nd primary. Fallback chain adjustments: sisyphus #1 kimi-k2.7-code → glm-5.2; momus pruned 2→1 (dropped kimi-k2.7-code); sisyphus-junior added kimi-k2.6 in middle; auditor #1 kimi-k2.6 → kimi-k2.7-code; post-reviewer added opencode/glm-5.2 in middle; unspecified-high fallback order swapped (glm-5.1 ↔ qwen3.6-plus); git-commit-message added zai/glm-4.5-air as fallback #2. modelConcurrency: opencode/kimi-k2.6 cap 2→3, opencode/kimi-k2.5 cap 3→2 (writing-category fallback tightened). ultrabrain category also moved to opencode-go/kimi-k3 (kimi-k2.7-code → kimi-k3; fallback #1 glm-5.1 → glm-5.2) — kimi-k3 now has 2 consumers (oracle + ultrabrain) on cap 2. §Agent Routing L204+L206 synced; SYSTEM-NARRATIVE Provider Strategy v3 + Current System State synced; pre-existing drift also corrected in L206 (ultrabrain category was miscounted as zai in the category tally — has always been opencode-go per L205; glm-5.1 narrative miscounted prometheus as zai — has always been opencode-go).
   Jul 19 Cleanup pass + validator workspace exemption. Removed CLEANUP.md (75-line June-era cleanup-policy doc; references stale `~/.config/opencode/.sisyphus/` path migrated away Jun 8, content duplicated by session-close + skill-creator skills which already enforce the retention). 2 inbound references redirected: skill-creator L284 + session-close L94 (file pointers dropped). Validator (`scripts/validate-skills.sh`): README.md check now exempts `skills/*-workspace/` — workspace skills hold eval iterations not skill bodies, so a README there documents workspace state, not redundant skill docs (the `fullstack-dev-workspace/README.md` WARN eliminated). Validator result: 52→53 PASS / 1→0 WARN / 0 FAIL.
   Jul 19 Pre-public PII scrub (Option A — docs-only) + benchmark openrouter stale-ref fix. Scrubbed `/home/<user>/` → `~/` (or relative path) across documentation prose and skill-body examples: SYSTEM-NARRATIVE.md (11 instances), AGENTS.md L268, COMPLETE-CODEBASE.md L115+L116, prompts/main-vault-context.md (2 instances), skills/system-reference/SKILL.md L228, skills/toolkit-session/SKILL.md L33, skills/security-auditor/SKILL.md L80+L567+L591, skills/shader-dev/test-results/shader-eval-harness.js L1 (hardcoded require → relative path). Out of scope (operational, left as-is): opencode.json L34-38 MCP paths, benchmark/tasks.json L4 repo_root. Out of reach (Layer 0 trust-root protected, operator must scrub manually): plugins/sisyphus-gates/{THREAT-MODEL.md L649, src/project-state.js L33, test/adversarial/g7-routing.test.js L149+L192+L200}. benchmark/tasks.json: 3 instances of `openrouter/deepseek/deepseek-v4-flash` → `opencode/deepseek-v4-flash-free` (operator confirmed they no longer use openrouter; aligns with current explore agent model).
   Jul 21 Post-audit FAIL → second-pass working-tree scrub. Re-audit caught what the first scrub + filter-repo missed. Operator accepted history leaks (Option γ — project names are test-run artifacts from skill calibration, not client work). Working-tree fixes: deleted tracked `skills/website-analyzer/.sisyphus/` (25 files; nested dir filter-repo's root-only `--path` had missed); deleted operator-local one-off scripts `get_rendered.js` + `detailed_inspection.js`; added `.sisyphus/` to `.gitignore` (catches nested dirs anywhere, not just root); scrubbed project names from 6 tracked files (SYSTEM-NARRATIVE.md v1.3.0/v1.5.0 test-target cells + L178-180 forensic-audit table → `project-alpha/beta/gamma` + "two test sites"; COMPLETE-CODEBASE.md L35 tasks list → generic; website-analyzer/strategy/test-integration.js L136+L146 test fixtures → `example.vercel.app` + `sample-clone`; ui-auditor/SKILL.md L145 + prd-writer/SKILL.md L229 → "representative test project"); fixed project-state.js L37 comment example output (operator-username → generic, matching the input basename — earlier scrub had updated the input but missed the output); scrubbed a project-name reference from `benchmark/results/2026-05-29-codegraph-run1.jsonl` (CAL-09 result referenced a deleted one-off script). Remaining intentional operator-path reference: `benchmark/tasks.json` L4 `repo_root` (operator-accepted Option A scope; cloners update to their own path). Lesson: filter-repo `--path` matches root-level only; use `--path-glob '**/<dir>/**'` for nested paths.
   Jul 21 README rewrite (public-flip prep). Old README was technically complete but governance-first in framing — opening with "governance system / cryptographic gates" undersold what the system actually does. New README: platform-first hook (46 skills that build things + governance that makes them trustworthy); 30-second version with two concrete examples (destructive-command block + signature-gated workflow advancement); new "What can it actually do?" section listing what skills produce (website cloning, full software projects, full-stack apps, shaders, PPTX, security audits, code review, UI audit, multi-session teaching, research); "What makes it safe?" demoted below the productive half; "Who is this for?" reframed to include "builders wanting a starting skill library"; new "How does this differ from typical agent safety patterns?" section comparing Sisyphus to confirm-each-action / process sandbox / RBAC patterns. Status footer refreshed (sisyphus-gates `v0.3.0+CLI+protocol`, active-maintenance link). New "License" placeholder section at end flags the missing top-level LICENSE file — operator must add one before public flip.
   Jul 21 Post-public-flip smoke test caught a latent test-hygiene bug + README count drift corrected. Smoke test: fresh `git clone` into `/tmp/opencode/public-clone-test/` → `npm install` → `npm test && npm run self-test`. First run failed AC-3.2 (`test/sandbox-integration.test.js:113`): the `inProdCwd()` helper used `process.cwd()` at load time as its "outside-sandbox" stand-in, assuming the repo was never cloned under `/tmp/` — breaks the moment anyone does (README's own Layer 3.7 example is `npm install in /tmp/opencode/some-test/`). Fix: `process.chdir(os.homedir())` instead of `originalCwd` (`homedir` was already imported but unused — original intent). The policy was correct; only the test was location-dependent. Verified: 12/12 sandbox-integration tests pass from `/tmp/` cwd after fix; full re-run 446 unit + 22 self-test PASS. Same session: README count drift corrected — 46→45 skills (5 places: tagline, "what the X skills produce", "starting skill library", §4 heading, tree comment) and 18→19 source modules (2 places: §1 prose, tree comment). Actual counts glob-verified (45 SKILL.md files; 19 .js in plugins/sisyphus-gates/src/); test-file count (21) and totals (446/22) were already correct. Two commits: `test(sisyphus-gates): fix AC-3.2 under /tmp/ clones` + `docs(readme): correct skill + source-module counts`. Lesson: the fresh-clone smoke test is the only way to catch location-dependent test assumptions the in-place run hides — it earned its cost.
   Jul 23 Catastrophic-defense bypass closure (GitHub #1) — deny-side, 6 finding-classes, 5 corpus-gated zero-FP pushes. The brain-2q4 P0 spec-lock corpus surfaced that Layer 1's catastrophic denylist returned {"blocked":false} for 6 bypass classes in approved state: F1 wrappers (env/time/nice/nohup/timeout/command/strace/… + rm), F2 structural ({rm,ls} brace / (rm) subshell), F3 newline injection (git status\nrm -rf /), F4 env-prefix smuggling (LD_PRELOAD/BASH_ENV/GIT_PAGER-exec/…), F6 PATH-qualified (/bin/rm). Root cause (F5): Layer 1 is command-name-based (extractCommandName returns the first token), not raw-substring — anything putting a non-catastrophic token in front of rm defeats it. Oracle-reviewed both sides (allow ses_070001de4ffeD2fa7xPu6d3FSN, deny ses_06fb8a7d9ffemdyffY2nnU7xmM). Fixes (each a separate push, all zero-FP): P-A hasShellMetachar newline/CR split + line-cont join + NUL (F3); extractCommandName basename-normalize /bin/rm→rm (F6); P-B1 isDestructiveCommand first-token strictness — reject (){}'"$`\\ in command name (F2 + obfuscation); P-B2 WRAPPER_SET wrapper-recursion (recurse + scan, flag-agnostic) + isSafeReadOnlyCommand isDestructiveCommand guard — env/time/timeout no longer Layer-4-allowed before Layer 6 (F1); P-C DANGEROUS_ENV_VARS Tier-1 denylist + VALUE_RULED_ENV/SAFE_PAGER_VALUES Tier-2 value-rule + hasDangerousEnvPrefix + isSafeReadOnlyCommand early guard + git-subcommand env-stripped-tokens fix (F4 + resolves 2 FP-over-blocks PAGER=cat). Corpus catastrophic-defense-bypasses.test.js: 81 cases, 0 todo. Suite 587 tests, 580 pass, 0 fail, 7 todo (brain-2q4 P2 must-allow, separate). Meta: the security-auditor skill missed F1–F4 across the 5 pre-public audits (it scans source, never runs gate-vs-corpus) — the corpus-first methodology is the real fix; the corpus itself caught the P-C polarity bug (isSafeReadOnlyCommand hasDangerousEnvPrefix return-true) mid-flight. Same session: git filter-repo --mailmap rewrote all 175 commits vladi<vladi@local> → TVATDCI<165805964+TVATDCI@users.noreply.github.com> (contribution attribution) + git config set forward.
    Jul 23 brain-2q4 P2 — Layer 4.5 compound-allow (additive allow-path for read-only compounds). isSafeCompoundCommand: charset reject + split + per-segment safe-readonly + non-destructive + sort/sed excluded + trust-root scan. gates.js Layer 4.5 between Layer 4/5. SAFE_GIT_SUBCOMMANDS + SUBCOMMAND_GIT.safe extended (ls-files). Suite 587/0/0 — zero todo across both test files. Live-verified: git status && git log works through the real gate; ls | sort -o file blocked. Refs brain-2q4.
   Jul 25 Reverse bridge initiative (R1-R4, parked from 2026-07-24 hotcache) — `skill:session-begin` added as 46th skill. The session-begin protocol that was a one-line AGENTS.md table row is now a formal 5-step skill: (1) read hotcache.md, (2) `bd memories` query, (3) git sweep (`git status` + `git log --since=<hotcache ts>` as separate Layer 4 bare calls — NOT a script, because session-begin runs outside any workflow phase and `bash scripts/git-sweep.sh` is Layer 4 blocked), (4) read pi handoff at `~/.pi/agent/exports/pi-handoff.md` if present, (5) surface pi's proposed bd facts for operator promotion (no auto-promote — bd writes are opencode's constitutional role). Two AGENTS.md Session Triggers bugs fixed: `bd remember` → `bd memories` (the former is write-only, errors on no-args; the read command is `memories`); `state.json` dropped from the agent instruction (Layer 0 trust-root protected — agents cannot read it regardless of phase; `hotcache.md` is the authoritative projection). R2-R3 pi-side: `skills/session-begin/PI-HANDOFF-SPEC.md` is the contract for pi to implement — schema mirrors hotcache.md + adds Dead ends + Proposed bd facts sections; pi writes at session-close; symmetric reverse of the forward `bd-bridge.ts`. R4: `pi:` commit prefix convention documented in spec (operator confirms with pi — current pi commits use standard conventional format without prefix). Validator: 46 PASS / 8 WARN / 0 FAIL (WARNs are pre-existing agent-file `name` field omissions). toolkit-lsp deferred to separate track. bd create was gate-blocked (Layer 4 — bd writes outside execution phase); used harness task_create for tracking instead — noted as AGENTS.md-vs-gate conflict for session-close.
   Jul 25 Finding A fix SHIPPED — SUBCOMMAND_BD reclassification in command-policy.js (operator removed gates so agent could edit trust-root source directly). Moved create, update, stale, orphans, lint, preflight from destructive → safe set (append-only record writes + read-only queries, no filesystem/shell impact). Kept destructive: close (evidence gate at shouldBlockCommand L404, asserted by fail-closed.test.js:310), forget (real deletion), dolt (Layer 6.5 separately gates bd dolt push), edit (interactive), setup, mol, defer, supersede, human. Updated adversarial test g1-g2-g3-command-policy.test.js:222-234 (destructive list pruned, safe list extended). Added new test file bd-classification.test.js (8 tests: 4 phase-agnostic-allow + 4 regression-guards). Suite: 587→595 unit + 22/22 self-test, 0 fail. Resolves the AGENTS.md-vs-gate normative conflict (AGENTS.md mandates bd for ALL tracking; gate had blocked bd writes outside execution phases, forcing a forbidden task_create fallback). AGENTS.md Beads section updated with phase-agnostic vs phase-gated subcommand policy. Oracle ses_0656dc708ffeOMNXBtw0kJm9Wh. Evidence: ~/.sisyphus/evidence/finding-a-bd-classification-fix-2026-07-25.md. Gates must be re-enabled by operator for the change to take effect at runtime.
   Jul 25 Oracle T4+T5+Finding-B shipped (gates temporarily disabled for source access). T5: TRUST_ROOT_READ_PATTERNS boundary fix — `sisyphus-gates/src/` regex → `src(?:\/|$)` so the directory itself (no trailing slash) is caught, not just files inside it. 5 boundary tests in trust-root-paths-opencode-json.test.js. T4: bd_remember.py category-enum warning added (catches typos, non-blocking) + THREAT-MODEL.md Section 6 note on interpreter-wrapping (subprocess-internal execution) as a known residual bypass class (scripts/bd_remember.py is the sanctioned wrapper). Finding B: hasShellMetachar quote-aware fix — raw regex `/(?:&&|\|\||[;|\n\r])/` replaced with `hasUnquotedChainOp` char-scanner tracking inSingle/inDouble quote state. Metacharacters inside quoted --value args (`python3 s.py --value "a;b"`) no longer false-positive-block. Interpreter -c payloads independently caught by isInterpreterDestructive (safe); shell wrappers (bash -c) caught by shellWrappersDestructive. 19 new adversarial tests in finding-b-quote-aware-metachar.test.js; 2 stale false-positive tests (B-quote-3 escaped ;, B-quote-4 quoted ;) removed from brain-2q4 corpus. Suite 600 to 617 (19 added, 2 removed), 22/22 self-test. Oracle ses_0656dc708ffeOMNXBtw0kJm9Wh.
   Jul 27 brain-hxm (P0) — bd destructive-set inverted-default hole. In execution phase, Layer 6 only blocks commands isDestructiveCommand() classifies as destructive; in fail-closed, Layer 5 blocks all non-safe bash regardless. Result: 11 dangerous bd subs absent from SUBCOMMAND_BD.destructive were allowed in execution phase (the only path that matters — it's where the agent is unleashed). Vectors: `bd sql 'DELETE FROM memories'` bypassed `bd forget` (the bead's headline); `bd hooks install` wrote executable git hooks (persistence — fires with operator privileges on next commit/push); `bd admin reset` wiped all beads data; `bd delete/prune/purge/gc` directly destroyed issues/history; `bd compact/flatten` irreversibly squashed Dolt history; `bd batch/import` wrote bulk to the database. Fix: extend SUBCOMMAND_BD.destructive enumeration by 11 — delete, sql, prune, purge, gc, compact, flatten, batch, import, admin (top-level cover — admin reset + every other privileged db-maintenance op), hooks (top-level cover — hooks install/uninstall). Pre-existing per Oracle ses_05fa90ad2ffe25lDSMkB0WImbE final-review catch (Finding A session); not introduced by Finding A. Agent cannot edit plugin source (Layer 0 trust-root) — workflow: agent writes RED tests in bd-classification.test.js (14 new tests: 11 enumeration + 3 named-vector regression + 7 safe-sub guards; tautological shouldBlockTool tests removed because test-env getCachedWorkflowConfig()=null blocks Layer 5 before Layer 6 is exercised), agent prepares source diff, operator temporarily disables gates, applies diff, re-enables gates, runs suite. AGENTS.md Beads section phase-gated list extended to match (was drifted — only listed 6 of the 9 actual destructive subs pre-brain-hxm). COMPLETE-CODEBASE.md timeline entry (this). SHIPPED via operator gate-disable cycle (operator lifted Layer 0, agent applied diff, operator re-enabled gate). Commits 23a94fa (canonical conventional-message) + 3979c82 (source) + 96f9c12 (AGENTS.md + timeline). Suite 648/0/0 after this and the #3 coverage work below. bd brain-hxm closed by operator.
   Jul 27 hotcache #3 — T5 + Finding B test coverage gaps. 7 new regression tests across 2 existing test files (no new files; suite 641→648). No source changes — defenses were already in place, only coverage was missing. T5 file (trust-root-paths-opencode-json.test.js, 20→22): dot-dot traversal regression (canonicalize() calls node:path resolve() to normalize `..` before the regex match — `sisyphus-gates/foo/../src/x` is caught); symlink evasion regression (canonicalize() calls realpathSync — creates tmp symlink → src/, confirms blocked, cleans up via finally/rmSync). Finding B file (finding-b-quote-aware-metachar.test.js, 22→27): case-4 squote-in-dquote (exercises hasUnquotedChainOp's !inDouble guard on the `'` branch — squote inside dquote stays literal, inDouble remains true, metachar-checking continues inside the dquote) + destructive variant; unclosed-dquote + unclosed-squote (scanner EOFs in quoted state, returns false; bash syntax-errors unclosed quotes anyway, so safe in practice); closed-form regression (`"foo"; rm -rf /` still caught). Tests added directly — `test/` is NOT trust-root protected (only src/, dist/, threat-model.md per TRUST_ROOT_READ_PATTERNS). README test count bumped 641→648 in 6 places (file count 25 unchanged). Commit 9130fb8. Lesson reinforced: agent asserted "commit still pending" earlier in session without checking git status — corrective discipline recorded as global:constraint:verify_git_state_before_asserting.
    Aug 2 Agent routing rebalance (operator-driven; oh-my-openagent.json modified in working tree — separate commit): sisyphus ultrawork variant glm-5-turbo → zai-coding-plan/glm-5.1 (aligns with the sisyphus primary tier); oracle fallback chain reshuffled — opencode-go/glm-5.2 promoted to fallback #1, opencode/glm-5.2 dropped (no longer referenced), opencode/kimi-k3 added (same provider tier as oracle primary). All 18 agent primaries + 9 category primaries unchanged; modelConcurrency map untouched (ultrawork now consumes the glm-5.1 cap). §Agent Routing L217 + Provider-mix L219 synced. Same day, first graph-engineering absorption deliverable landed: wave-executor Step 3c verify-checklist item 7 (graph-node traceability / fan-out gate) — the execution-receipt mandate from ABSORPTION-DRAFT.md §14 missing-piece #4 (skills/wave-executor/SKILL.md:385).
    Aug 2 (session 2) Agent routing consolidation to a 4-model zai map (operator-driven; oh-my-openagent.json modified in working tree — separate commit, supersedes the 3e9ac81 ultrawork→glm-5.1 step same-day): glm-5.1 + glm-4.5-air retired entirely from zai primaries; ultrawork reverted glm-5.1 → glm-5-turbo (back to Jul 17 state); prometheus, momus, auditor, reviewer, fullstack-dev-tester moved glm-5.1 → glm-5.2; post-reviewer moved glm-5-turbo → glm-5.2; metis moved glm-5.1 → glm-5-turbo (+ hy3-free fallback #3); categories visual-engineering + unspecified-high glm-5-turbo → glm-5.2, deep + artistry glm-5.1 → glm-5.2, quick glm-4.5-air → zai deepseek-v4-flash-free. Fallback chains reworked onto the 4-model zai spine: explore [minimax-m2.7, zai glm-4.7] (glm-4.5-air dropped), explorer [minimax-m2.7, ling-3.0-flash-free, zai glm-4.7], atlas/sisyphus-junior/unspecified-low/writing/git-commit-message swapped hy3-free/kimi-k2.5/mimo-v2.5-free → ling-3.0-flash-free/deepseek-v4-flash-free; auditor/post-reviewer/fullstack-dev-tester/reviewer glm-5.1-fallbacks → glm-5.2/kimi-k2.7-code. modelConcurrency: zai glm-5.2 2→5 (12 consumers: 7 agents + 5 categories), zai glm-5.1 + glm-4.5-air entries removed, opencode/hy3-free → opencode/ling-3.0-flash-free (3); map now 21 entries (3 zai + 8 opencode-go + 10 opencode). Net provider tally: zai 14/18 agents + 8/9 categories; opencode-go 2 agents + 0 categories (oracle — kimi-k3, multimodal-looker — mimo-v2-omni); opencode 2 agents + 1 category (explore, explorer, git-commit-message — all deepseek-v4-flash-free). §Agent Routing L217-L220 + Provider-mix L220 synced; SYSTEM-NARRATIVE Current System State + Provider Strategy v3 synced. Two cosmetic nits documented: opencode/kimi-k2.5 dead entry (writing dropped it) + opencode/hy3-free uncapped (still referenced by hephaestus + metis fallback #3).
   Aug 3 brain-tm7 Oracle steps 1-3 SHIPPED (commits 4c9b2cb + 9982546, pushed to origin). Step 1 receipt mechanics: wave-executor SKILL.md:385 item 7 → parallel fan-out receipt gate at `$HOME/.sisyphus/evidence/execution-receipts.jsonl` (NOT project-relative); wave-validator.sh new check #7 (L99-129) — declared fan-out ⇒ receipt log must exist with ≥ node count, else FAIL. Step 2: skills/orchestrator-review/SKILL.md (47th skill, 326L) — operator-owned fork of plugin `review-work`; 6 gate lanes (goal-verify oracle, code-review, security-auditor, conditional ui-auditor/regression-gate, oracle second-opinion Phase 2.5), barrier-before-report (L211), no model literals (routing abstractions only — `category`/`subagent_type`); registered COMPLETE-CODEBASE L25 (46→47 dirs) + L27. Step 3 vocab/budget: AGENTS.md Graph Shapes section L35-43 (chain/diamond/barrier per graph-engineering §15, diamond-vs-barrier merge rule, receipt-per-node-before-merge), judging-node fallback rule L54 (§17 #3 — review/audit/momus/oracle/post-reviewer prune cheap models even as fallback), graph token multiplier L55 (§17 #2 — N× budget, collapse-to-chain escape); plan-writer SKILL.md Graph Shape per-slice requirement L257-273. Bonus pre-existing bug fix (9982546): wave-validator check-5 UI heuristic — bare `*ui*` matched "build" (b-ui-ld), contradicting check-3 which requires `*build*` evidence (mutually exclusive without DESIGN.md); anchored to `*ui-*`/`*ui_*`. Validator verified live by operator: `bash -n` clean, check #7 PASS (3 receipts ≥ 3 nodes), check-5 both directions. Execution receipts filed (first 3 ever: task-1/2/3, counter=3 — `~/.sisyphus/evidence/execution-receipts.jsonl`). Post-reviewer judging chain confirmed clean (no cheap model — §17 #3 satisfied via 3e9ac81 rebalance). Graph-engineering absorption source: `~/developer/ytdl/graph-engineering/` §11 (orchestrator), §15 (vocab), §17 (caveats).

   Aug 3 (operator config tweak) opencode/hy3-free retired entirely. hephaestus + metis fallback #3 swapped opencode/hy3-free → opencode/laguna-s-2.1-free (operator trade; both chains also gained opencode/deepseek-v4-flash-free as fallback #4); quick category added opencode/laguna-s-2.1-free as fallback #3. modelConcurrency: opencode/laguna-s-2.1-free=3 added; opencode/kimi-k2.5 removed (dead — unreferenced since Jul 19 writing-category tightening; resolves the Aug 2 'Two cosmetic nits' both). hy3-free now zero references + zero cap. Net modelConcurrency unchanged: 21 entries (3 zai + 8 opencode-go + 10 opencode). §Agent Routing Provider-mix L228 + SYSTEM-NARRATIVE Provider Strategy v3 L259 synced.

   Aug 3 (process) Doc-drift guard relaxed — routine model swaps (agent/category primary-model changes, fallback-chain reshuffles, modelConcurrency cap tweaks, free-tier retirements/additions) are now commit-message-only: no timeline entry, no current-state prose sync. oh-my-openagent.json is the source of truth; prose summaries refresh on architectural change, not per-swap. Timeline + prose sync stays mandatory for architectural/decision changes (new skills/agents, gate layers, canonical paths, permissions, workflow, process changes, non-obvious decisions). Rationale: LLM landscape evolves fast — model swaps are frequent + low-decision-content; per-swap prose chase was high-cost/low-value (this session's hy3→laguna swap surfaced it). Encoded in AGENTS.md Doc Drift Guard + session-close SKILL.md step 3. Prior model-swap timeline entries (e.g. the hy3 entry above) grandfathered as historical records.

   Aug 3 (session 2) brain-tm7 Oracle items 4-5 SHIPPED (commit 0b54077, pushed; bead brain-3dv CLOSED). Item 4 wave-validator check #7 wave-scoping (Oracle Q5 [MEDIUM]): FANOUT regex tightened from `fan-out|fan_out|delegated N parallel` to `fan-out[[:space:]]+[0-9]+|fan_out[[:space:]]+[0-9]+|delegated[[:space:]]+[0-9]+[[:space:]]+parallel` — bare "fan-out" mentions (incl. "no fan-out was needed") no longer trip FANOUT_DECLARED with NODES=1; explicit node declaration required. FOUND_ENTRIES scoped from global cumulative `grep -c '"n":'` to plan-bead `grep "\"bead\":\"${PLAN_NAME}\"" | grep -c '"n":'` — eliminates the trivial-pass-after-5-receipts failure mode (gate now verifies THIS plan filed receipts, not that receipts exist somewhere). DECLARED_NODES extraction `head -1` → `sort -n | tail -1` (takes MAX declared count, most conservative). Residual: intra-plan cross-wave accumulation (wave 1 of plan X counts toward wave 2's check) — documented inline at scripts/wave-validator.sh L99-106 + bd key `wave_validator_check7_scoping_2026_08_03`; future improvement is to snapshot the receipt counter at wave start and require `n >` that value. Item 5 nits batch: scripts/check-doc-claims.sh --self-test sed retarget 17→18 agents (first "17 agents" had landed on CC L96 historical "13 of 17 agents" — guard's Pattern 2 didn't extract the historical line, so the breaking step false-FAILed); CC L214 stray bare `zai` token removed (Jul 1 rename artifact); CC L145 Provider-mix pointer L223→L226 (was already stale at L227 pre-cleanup; shifted -1 by the L214 removal); scripts/pre-push.sh echo labels 180 tests/~400ms→648/~5s + 20→22 scenarios; skills/orchestrator-review/SKILL.md Phase 2 tracking table — added note that Lane 6 (Oracle Second-Opinion) is conditional Phase 2.5 (fires only on high-risk trigger), tracked in Phase 3 final report (not Phase 2 barrier table). Operator-side: ghost ~/.config/opencode/.sisyphus/ removed (single duplicate of canonical ~/.sisyphus/evidence/session-close-2026-08-03-brain-tm7.md, left by prior session-close using a project-relative path; unblocks scripts/check-completion-honesty.sh Ghost path check). Validators: check-doc-claims.sh + --self-test ALL PASS, check-completion-honesty.sh PASS (0/0), bash -n wave-validator.sh clean. Receipt: task-6 (delegation to category=git-commit-message — gate blocked agent-initiated git add/commit outside execution phase by design; subagent wrote verbatim 36-line commit message to /tmp/opencode/commit-msg-brain-3dv.txt; operator ran git add + git commit -F from terminal).
   Aug 4 TUI verdict-parser leak fix + v1→v2 schema patch (Oracle ses_0385555baffeJtl0dkqA5OOGWy, bg_19fdf143). plugins/sisyphus-gates/src/verdict-parser.js: SUPPORTED_SCHEMA_VERSION 1.0.0→2.0.0 (root cause: unsigned scanReviewFiles path was v1-only while loadSignedVerdicts + disk emitter had moved to v2; drift surfaced as repeating "invalid verdict" warning in TUI on every render). Numeric semver compare (isNewerVersion — replaces lexicographic string > which classified "10.0.0" as not-newer-than "2.0.0" via '1' < '2' at index 0; latent v10 bug). v2 field aliasing in parseVerdictBlocks (timestamp ← timestamp ?? signed_at; reviewer ← reviewer ?? operator — semantically lossy but bounded to FAIL-only repair-brief.js path) + additive pass-throughs (id/sessionID/signed_at/operator — flows through scanLatestVerdicts' {...v} spread). plugins/sisyphus-gates/src/review-scanner.js: cosmetic patch by user (console.warn → logGateEvent + module-level Set dedup) silenced the TUI spam; not by this agent. Suite 648/648 PASS (no regressions; additive fields tolerated by existing tests). Runtime smoke test 20/20 (incl. real disk file that was the leak source + v10 semver fix + v1 backward compat). **Architecture finding (non-obvious):** state.js:211-238 already uses loadSignedVerdicts (signed, verifies SISYPHUS_GATE_SIG + MEMORY_KEY) as AUTHORITATIVE for gate=PASS; unsigned scanReviewFiles is bounded to defensive FAIL-downgrade only (DoS vector, not bypass). The 3 June v2 PASS verdicts on disk (absorption-001, sandbox-allowlist ×2) were already determining planGateStatus=PASS via signed path before this patch — patch does NOT activate any new PASS. Oracle's recommended Medium follow-up (make signed path authoritative) is ALREADY implemented. MEMORY_KEY missing → loadSignedVerdicts gate=null → fail-closed (correct). Operator disabled Layer 0 trust-root gate (opencode.json) for plugin source access; operator to re-enable post-push. Layer 4 bd: schema_alias_reviewer, verdict_error_delimiter, unsigned_parser_trust, stale_pass_activation, signed_path_already_authoritative, lsp_tsserver_path_config (follow-up). Evidence: ~/.sisyphus/evidence/session-close-2026-08-04-verdict-parser-v2-schema.patch.md.

   Aug 10 oh-my-openagent upgraded 4.18.2 → 4.19.4: crossed the Jul 17 stop-short-of-4.19.0 line after scoping showed the concern was over-conservative. Two parallel explore sweeps confirmed: ZERO `shared/`-alias skill references and ZERO ralph-loop references in repo files (excluding node_modules). The `shared/` prefix in the runtime skill registry is plugin-generated, not repo-authored; repo `load_skills` arrays all use bare names. `skills/_shared/` is local reference material (1 file: a LNK artifact), not loaded via the plugin's dropped `shared/` alias machinery. Ralph → Goal: zero repo exposure (release-note promise of auto-migration holds). 4.19.0 breaking refactor (`refactor!: drop shared/ skill aliases and protections`) does not touch any repo file. Version pins bumped in 5 files: package.json, opencode.json (operator-side — Layer 0 trust-root protected, agent cannot write), tui.json, README.md, ci.yml canary (stale 4.7.5→4.19.4). Verified: verify-plugin-compat PASS (0 fail/0 warn), 648/648 unit + 22/22 self-test PASS, delegate-task contract smoke-tested OK (4.19.0 commit 79004bf changed the contract — removed duplicate available_skills from non-plan subagent prompts). Watch-items: (1) 11 npm audit vulns (4 low/3 mod/4 high); (2) skills/_shared/ loader behavior on next opencode restart; (3) Aug 10 SYSTEM-NARRATIVE.md:411 "operator intent to stop using oh-my-openagent" reversed same-day by operator decision. v5.0.0-beta.1 available but declined (breaking omo→omo-agent-toolkit rename). **Config architecture migration (4.19.4):** 4.19.4 introduced unified config at `~/.omo/omo.jsonc` (wrapped in `[opencode]`). Migration auto-ran from `oh-my-openagent.json` but had TWO bugs for `[opencode]` agents: (1) merged `model`+`fallback_models` into `models` arrays — but `AgentOverrideConfigSchema` only accepts `model` (singular) + `fallback_models`, NOT `models` (categories accept `models`, agents do NOT); (2) missed the nested `sisyphus.ultrawork` sub-block entirely. Result: 22 doctor issues (19 Unknown keys) → plugin rejected agents section → fell back to built-in defaults (claude-opus-5 / kimi-k3 — the "big models" symptom). Manual fix via /tmp/ node script: agents back to `model`+`reasoning`+`fallback_models`; ultrawork to `model`+`reasoning` only (ultrawork sub-schema accepts neither `models` nor `fallback_models`). 18 deprecation warnings remain (`fallback_models` deprecated at agent level but the only valid key — doctor's "replace with models" advice is buggy for 4.19.4's agent schema; confirmed via `AgentOverrideConfigSchema` Zod source). **npm audit fix BANNED in this repo:** operator's `npm audit fix` broke the exact pin (`"4.19.4"`→`"^4.19.4"`) and deleted `oh-my-openagent.json` via a lifecycle script side-effect (recovered via `git restore`). **Fallback chain verified end-to-end:** oracle primary `opencode-go/kimi-k3` → quota exhausted → fell through to `zai-coding-plan/glm-5.2` (confirmed via TUI background-task model display — programmatic task metadata shows the initial route, NOT the post-fallback model). Legacy `oh-my-openagent.json` renamed to `.bak` (4.19.4 ignores it — reads from `~/.omo/omo.jsonc`). **Open (pre-push):** scripts `check-completion-honesty.sh` (Check 5 agent/category counts + Check 9 routing-claim match) and `check-doc-claims.sh` (agent/category count via `node -e require(...)`) still read the old `oh-my-openagent.json` path — need updating to read `~/.omo/omo.jsonc` with JSONC-aware parsing before the next push. (Resolved Aug 10 post-entry by commits a8d5ba0 + af5b05f — validators now read `~/.omo/omo.jsonc` via the JSONC-aware helper.)

   Aug 14 4.19.4 category-routing repair + glm-5.3 adoption (operator-driven config edits; timeline entry for the non-obvious repair, prose-only sync for the swap per doc-drift guard). Repair: the Aug 10 migration had ALSO rewritten `[opencode].categories` into `models`-chain format — but 4.19.4 runtime readers only honor legacy fields (`available-categories.ts` reads singular `.model`; `runtime-fallback/fallback-models.ts` reads `.fallback_models`), so all 9 categories silently fell to builtin DEFAULT_CATEGORIES requirement ladders (claude-fable-5 / gpt-5.6-sol / kimi-for-coding-highspeed — the TUI Models-panel mismatch + opencode-zen billing symptom). Top-level `categories` is CLI-doctor-only, not runtime — placing it there (first attempted fix) changed nothing at runtime. Final fix: categories restored INSIDE `[opencode]` in legacy `model`+`fallback_models` format (verbatim from the 4.18.2 backup); TUI panel verified post-restart with all 12 lanes matching pinned config. CONSTRAINT (bd `omo_no_models_chain_format`): never convert `[opencode]` agents/categories to `models`-chain format — doctor's "replace fallback_models with models" advice is wrong for the `[opencode]` runtime schema, and `config migrate` is a no-op here (`_migrations` markers already recorded). glm-5.3 adoption (operator, docs.z.ai release): sisyphus / momus / reviewer / post-reviewer primaries + ultrabrain category glm-5.2 → glm-5.3 (chains: zai → opencode-go/glm-5.3 → opencode/glm-5.2); oracle fallbacks glm-5.2 → glm-5.3; zai glm-5.3 modelConcurrency 3 (5 primary consumers — cap-starvation fallback to opencode-go/glm-5.3 possible in parallel fan-outs, acceptable, uncapped rung); ling-3.0-flash-free dropped from quick + unspecified-low (cap removed; still in writing + git-commit-message); opencode/kimi-k2.6 cap entry removed. Verified: `opencode models` lists every referenced model ID across all three providers; doctor parses clean (27 pre-existing deprecation warnings only, zero new). §Agent Routing L229-L231 + SYSTEM-NARRATIVE Current System State L368 synced; lane-by-lane manual diff vs `~/.omo/omo.jsonc` = 18/18 agents + 9/9 categories match. check-completion-honesty.sh / check-doc-claims.sh not agent-runnable this session (bash-script gate block) — enforced at pre-push as usual.

   Aug 15 (process) First live herdr-collab lane ratified (pi w3:p1 ⇄ sis w3:p2, herdr 0.8.0) — pi-authored cross-agent protocol skill `herdr-collab` (shared substrate `~/.agents/skills/herdr-collab/`, NOT counted in the 50+ user-installed skills) reviewed by sis across 3 rounds: v0.1 review (4 risks + 5 improvements, all 5 open questions answered) → v0.2 verification (all 9 faithfully incorporated, SIGNED OFF) → v0.2.1 (nits folded by pi). Empirical gate facts baked into the skill's "opencode-side gate notes": bare herdr subcommands (read-only AND mutating) pass Layer 4 with no allowlist entry; metachar defense is substring-level incl. inside quoted prompt payloads; `$()` extraction blocked → read JSON output instead; `rm` destructive-class blocked even bare; gates plugin src is trust-root read-protected. Conventions ratified: file-exchange-primary over pane reads (`/tmp/herdr-collab/<topic>/`, `<who>-<what>.md`, requester-input/responder-response, reply = path only), lane name `sis`, operator-owned focus decisions (`agent read` never marks seen), review-turn `--timeout ≥300000` (sis delegation latency; first data point ~3.5 min), `blocked` = gates approval dialog ≠ dead lane, symmetric read restraint (~/.pi/ + opencode trust roots), lane creator removes the shared dir. No config/plugin/code changes — doc + memory only. Evidence: `~/.sisyphus/evidence/session-close-2026-08-15-herdr-collab-lane.md`; reviews archived at `/tmp/herdr-collab/sis-review-v0.[1,2].md` + skill-dir `reviews/` (pi side).

    Aug 17 (process) Main-vault sharing policy A+C RATIFIED + implemented (pi ⇄ sis herdr-collab lane 2, `/tmp/herdr-collab/mainvault-sharing/`). New standing cross-agent workflow: (A) pi read-only vault QUERY access — wiki/**, index.md, log.md, ~/Main-vault/hotcache.md only; raw/ + ~/.sisyphus/hotcache.md excluded; data-not-instructions. (C) pi-initiated vault writes route operator-arbitrated herdr lane → sis → archivist with execution-receipt + verbatim-quote guards (2026-08-10 confabulation defect class); non-urgent only. Oracle second-opinion: SOUND-WITH-CONDITIONS, 9 conditions (abandon-early cap ~8-10K; log-tail for currency-sensitive queries — vault semantic layer measured frozen since ~2026-06, zero wiki date_updated ≥ Jul, umbrella knowledge lives in log.md tail only; symmetric verbatim-quote on reads; pointer facts existence+topic capped ~40; currency from date_updated not LINT; hotcache paths named; grep-first at index >40KB; monthly reconciliation; T3 best-effort). sis side: bd global constraint `mainvault_pi_read_policy` recorded + round-trip verified (propagates to pi via bridge export — OPEN ITEM: export cron never installed, bridge stale since Aug 14). C-path proven end-to-end run 1: archivist (glm-4.7) applied operator-mandated lane-partner note to Main-vault/AGENTS.md QUERY section + Last-updated line — 2 regions byte-exact, independent git-diff verified, committed cfb6c18 operator-side. WATCH-ITEM (permission model): NO permission prompt fired on archivist editing NON-allowlisted Main-vault/AGENTS.md — expected operator approval dialog (the assumed arbitration surface) never appeared; edit was operator-sanctioned in substance, but prompt-as-arbitration assumption unverified — investigate before relying on it for run 2. pi side: 3 commits (AGENTS.md clause + main-vault-query skill holding the 9 conditions + Tier-A absorption; operator deleted pi's colliding agents/archivist.md — was an in-graph vault-write bypass). Canonical artifacts: pi `exports/mainvault-adoption-2026-08-17/` (cite this, not /tmp — lane dir removed at close).

 3. Complete Workflow — 9-Phase State Machine
   ╔══════════════════════════╗
   ║ GLOBAL BLOCKING RULES ║
   ║ • Destructive cmds denied║
   ║ • Fail-closed on no state║
   ║ • Gate fail = all blocked║
   ║ • Unapproved = no commit ║
   ║ • Trust-root paths locked║
   ║ • HMAC signing required ║
   ╚══════════════════════════╝

┌────────────┐ auto on ┌────────────┐ auto on PRD write ┌────────────┐
│ Discovery │──skill:prd──►│ PRD-Writing│──────────────────────►│ PRD-Review │
│ (no gates) │ writer or │ (no gates) │ path contains "prd" │ ⛔ GATE │
│ │ user approve│ │ │ │
└────────────┘ └────────────┘ │ momus-prd │
│ reviewer │
│ │
│ Blocks: │
│ write/edit │
│ git commit │
│ git push │
└─────┬──────┘
│ Operator signs verdict:
│ node cli.js sign-verdict prd <id> PASS
│ → HMAC-signed artifact written
│ → loadSignedVerdicts verifies
│ → prd_gate=PASS, prd_approved=true
▼
┌────────────┐ auto on ┌────────────┐ manual ┌──────────────┐
│Issue-Create│──skill:plan──►│Plan-Writing│──approve──►│ Plan-Review │
│ (no gates) │ writer or │ (no gates) │ │ ⛔ GATE │
│ │ user approve│ │ │ │
└────────────┘ └────────────┘ │ momus-plan │
│ reviewer │
│ │
│ Blocks: same │
└──────┬───────┘
│ Operator signs verdict:
│ node cli.js sign-verdict plan <id> PASS
│ → HMAC-signed artifact written
│ → plan_gate=PASS, plan_approved=true
│ Then operator approves:
│ node cli.js approve <id>
│ → approval_status=approved
▼
┌──────────────────────┐ evidence ┌──────────────┐ regression ┌────────────┐
│ Execution (wave-exec)│──self-loop──► │ Validation │──script────► │ Close │
│ ┌──────────────────┐ │ logs evidence │ (regression- │ gate.sh │ ⛔ GATE │
│ │ Wave N │ │ │ gate.sh) │ │ │
│ │ implement slice │──► checkpoint-3 │ │ │ │ Blocks: │
│ │ log evidence │ │ (between-wave)│ │ │ │ bd close │
│ └──────────────────┘ │ │ │ │ │ unless │
│ ┌──────────────────┐ │ │ │ │ │ evidence │
│ │ Wave N+1 │ │ │ │ │ │ logged │
│ │ implement slice │──► checkpoint-3 │ │ │ │ │
│ │ log evidence │ │ (between-wave)│ │ │ │ │
│ └──────────────────┘ │ │ │ │ │ │
│ bash blocked │ │ │ │ │ │
│ (non-destructive only)│ │ │ │ │ │
└──────────────────────┘ └──────────────┘ └────────────┘
Phase Details

# Phase Skill Gates Blocks Advance Trigger

1 discovery discovery-orchestrator none none skill:prd-writer or user approval
2 prd-writing prd-writer none none Auto: path contains "prd"
3 prd-review ⛔ momus-prd-reviewer HMAC-signed verdict (cli.js sign-verdict) write, edit, git commit/push Operator signs: node cli.js sign-verdict prd <id> PASS
4 issue-creation issue-creator none none Auto: path contains "plan"
5 plan-writing plan-writer none none Manual: plan_written
6 plan-review ⛔ momus-plan-reviewer HMAC-signed verdict (cli.js sign-verdict) write, edit, git commit/push Operator signs: node cli.js sign-verdict plan <id> PASS, then: node cli.js approve <id>
7 execution wave-executor checkpoint-3 (between waves) bash (non-destructive only) Manual: wave approval; self-loop on evidence
8 validation regression-gate script: regression-gate.sh (exit 0) none Manual: user_confirms_validation
9 close ⛔ plan-closer evidence_check: evidence_logged==true bd close unless logged Manual: user_confirms_close
Agent Routing (18 runtime agents × 9 categories)
18 Named Agents (runtime): sisyphus (zai-coding-plan/glm-5.3, high; ultrawork: zai-coding-plan/glm-5-turbo, high), hephaestus (zai-coding-plan/glm-4.7), oracle (opencode-go/kimi-k3, high), librarian (zai-coding-plan/glm-4.7), explore (opencode/deepseek-v4-flash-free), multimodal-looker (opencode-go/mimo-v2-omni), prometheus (zai-coding-plan/glm-5.2), metis (zai-coding-plan/glm-5-turbo), momus (zai-coding-plan/glm-5.3), atlas (zai-coding-plan/glm-4.7), sisyphus-junior (zai-coding-plan/glm-5-turbo), archivist (zai-coding-plan/glm-4.7), athena (zai-coding-plan/glm-4.7), auditor (zai-coding-plan/glm-5.2), explorer (opencode/deepseek-v4-flash-free), post-reviewer (zai-coding-plan/glm-5.3), reviewer (zai-coding-plan/glm-5.3), fullstack-dev-tester (zai-coding-plan/glm-5.2)
9 Categories (via task(category='...')): deep→zai-coding-plan/glm-5.2, ultrabrain→zai-coding-plan/glm-5.3, visual-engineering→zai-coding-plan/glm-5.2, quick→zai-coding-plan/deepseek-v4-flash-free, unspecified-high→zai-coding-plan/glm-5.2, unspecified-low→zai-coding-plan/glm-4.7, writing→zai-coding-plan/glm-4.7, artistry→zai-coding-plan/glm-5.2, git-commit-message→opencode/deepseek-v4-flash-free
Provider mix: 3-tier fallback strategy (Jun 25 reset; Jul 17 model refresh + concurrency hardening). Tier 1 = zai-coding-plan subscription (primary, prioritized). Tier 2 = opencode-go lite subscription (used when zai-coding-plan busy or quota-exhausted). Tier 3 = opencode pre-pay-as-you-go (free + paid models, used when both subscriptions exhausted). Background: prior mixed-provider scheme (opencode-go primary, zai-coding-plan fallback) failed because OpenCode does not honor zai-coding-plan as a cross-provider fallback target — when opencode-go exhausted, OpenCode forced fallback to opencode zen (paid extra) instead of the configured zai-coding-plan chain. Solution: explicit 3-tier chain with opencode-go positioned between zai-coding-plan and opencode pre-pay so OpenCode's in-tier fallback behavior is respected. Agents on zai-coding-plan primary (14 of 18: sisyphus, hephaestus, librarian, momus, atlas, sisyphus-junior, archivist, athena, auditor, post-reviewer, fullstack-dev-tester, prometheus, metis, reviewer); opencode-go (2 of 18: oracle — kimi-k3, multimodal-looker — mimo-v2-omni); opencode (2: explore — deepseek-v4-flash-free, explorer — deepseek-v4-flash-free). Categories: zai-coding-plan (8: visual-engineering, ultrabrain, deep, artistry, quick, unspecified-low, unspecified-high, writing); opencode-go (0); opencode (1: git-commit-message). zai-coding-plan model concurrency (primary consumers, Aug 14 glm-5.3 adoption): glm-5.3 (concurrency: 3) — [MOST LOADED zai model per cap] primary for 4 agents (sisyphus, momus, post-reviewer, reviewer) + 1 category (ultrabrain) = 5 consumers + oracle fallback #2; glm-5.2 (concurrency: 5, raised from 2) — primary for 3 agents (prometheus, auditor, fullstack-dev-tester) + 4 categories (visual-engineering, deep, artistry, unspecified-high) = 7 consumers; the Jul 19 glm-5.1 spread consolidated back onto glm-5.2 (prometheus, momus, auditor, reviewer, fullstack-dev-tester moved glm-5.1 → glm-5.2; post-reviewer moved glm-5-turbo → glm-5.2; visual-engineering + unspecified-high categories moved glm-5-turbo → glm-5.2; deep + artistry categories moved glm-5.1 → glm-5.2); glm-5-turbo (concurrency: 3) — primary for 2 agents (metis, sisyphus-junior) + sisyphus ultrawork variant; glm-4.7 (concurrency: 5) — primary for 5 agents (hephaestus, librarian, atlas, archivist, athena) + 2 categories (unspecified-low, writing) = 7 consumers; zai deepseek-v4-flash-free (no explicit cap → zai providerConcurrency 10) — primary for 1 category (quick, moved off glm-4.5-air). zai glm-5.1 and glm-4.5-air entries removed from modelConcurrency (no longer referenced). opencode-go model concurrency: kimi-k3 (2) — SOLE consumer is oracle primary (premium reasoning model; most expensive Go-tier model at $3/$15 per 1M, 140 req/5hr upstream cap, hence cap 2); glm-5.2 (2) — fallback #1 for prometheus + fullstack-dev-tester; glm-5.3 (no explicit cap → opencode-go providerConcurrency 10) — fallback #1 for sisyphus, momus, post-reviewer, reviewer, ultrabrain + oracle (added Aug 14); glm-5.1 (3) — fallback-only since Aug 2 (hephaestus #1, metis #1, deep #1, artistry #1, unspecified-high #1, sisyphus ultrawork fallback); minimax-m2.7 (5) — explore/librarian/athena/quick/unspecified-low/writing/git-commit-message fallbacks (explorer moved off Aug 14 session 2); deepseek-v4-flash (no explicit cap → opencode-go providerConcurrency 10) — explorer fallback #1 (added Aug 14 session 2, replacing minimax-m2.7); mimo-v2-omni (1) — multimodal-looker primary; kimi-k2.7-code (2) — fallback-only since Aug 2 (sisyphus-junior #1, visual-engineering #1, auditor #1, post-reviewer #3, reviewer #2, fullstack-dev-tester #2); kimi-k2.6 (2) — atlas/archivist/sisyphus-junior fallbacks; qwen3.6-plus (1) — hephaestus/metis/visual-engineering/unspecified-high fallbacks. opencode model concurrency: deepseek-v4-flash-free (5 — capped on Jul 17 to protect the free tier; primary for explore + explorer + git-commit-message), ling-3.0-flash-free (cap removed Aug 14 when quick + unspecified-low dropped it; still fallback for atlas, writing + git-commit-message — explorer dropped it Aug 14 session 2; originally NEW Aug 2 replacing hy3-free as the generic opencode flash fallback), glm-5.2 (2), glm-5.1 (3), mimo-v2.5-free (3), gemini-3.1-pro (1), gemini-3-flash (1), kimi-k2.7-code (2), kimi-k3 (2). modelConcurrency map is now COMPREHENSIVE: 21 entries spanning all 3 providers (4 zai + 8 opencode-go + 9 opencode) — nearly every referenced model has an explicit per-model cap; only opencode/gpt-5.4 is no longer referenced (oracle fallback rewritten Jul 17 from [gpt-5.4] to [zai-coding-plan/glm-5.2, opencode-go/kimi-k2.7-code]). Both opencode-tier nits resolved Aug 3: opencode/kimi-k2.5 removed (was dead — unreferenced since the Aug 2 writing-category tightening), opencode/hy3-free retired (hephaestus + metis fallback #3 → opencode/laguna-s-2.1-free, now capped at 3). Fallback chain coverage (Aug 2 final state): ALL three Jun 25 gaps (atlas, archivist, athena) are CLOSED — every agent now has fallback_models; explore + explorer are both opencode-primary (inverted vs the 3-tier strategy, kept because deepseek-v4-flash-free is free and abundant); auditor is zai-primary. Capability notes: visual-engineering dropped its vision-specialized primary (glm-5v-turbo, removed Jul 17) for glm-5.2 (text) with vision fallbacks (opencode/gemini-3.1-pro); multimodal-looker on opencode-go/mimo-v2-omni (vision-capable omni model — capability fit); oracle on opencode-go/kimi-k3 (premium reasoning model — most expensive Go-tier model at $3/$15 per 1M, 140 req/5hr upstream cap, hence cap 2). runtime_fallback enabled with retry_on_errors [400, 401, 403, 404, 429, 500, 502, 503, 504], cooldown_seconds 15, timeout_seconds 10, notify_on_fallback true, restore_primary_after_cooldown true. Concurrency semantics (verified against oh-my-openagent source — priority chain is modelConcurrency > providerConcurrency > defaultConcurrency(10) > hardcoded 5; defaultConcurrency is keyed per-model NOT global; value 0 = unlimited): CRITICAL distinction — hitting a concurrency cap QUEUES the task; it does NOT trigger runtime_fallback. The 3-tier fallback chain only fires on HTTP errors (429/5xx) from the API. This works for quota exhaustion (zai returns 429 → fallback to opencode-go) but NOT for concurrency saturation (glm-5.1 cap=4 → 5th request queues, does NOT skip tier). restore_primary_after_cooldown: true means after cooldown_seconds (15s) the runtime retries the primary zai model instead of staying on the fallback tier.

18 Agents — plugin-bundled vs user-defined split (security boundaries)
Total: 18 agents. **8 user-defined** (have a `.md` in `~/.config/opencode/agents/`: oracle, archivist, athena, auditor, explorer, reviewer, post-reviewer, fullstack-dev-tester — these own identity, mode, temperature, permission block, and behavioral prompt). **10 plugin-bundled** (NO `.md` in `agents/`; sourced from the oh-my-openagent plugin install dir at `<installPath>/agents/`: sisyphus, hephaestus, librarian, explore, multimodal-looker, prometheus, metis, momus, atlas, sisyphus-junior — these own their prompt bodies internally; the JSON overlay in `oh-my-openagent.json` supplies only model + fallback + variant + prompt_append routing). Note: `explore` (plugin-bundled, opencode/deepseek-v4-flash-free, cheap fast contextual grep) and `explorer` (user-defined, opencode/deepseek-v4-flash-free, capable research specialist with websearch/webfetch) share the same primary model; their distinction is in agent definitions, tool permissions, and fallback chains — explore falls back opencode-go/minimax-m2.7 → zai-coding-plan/glm-4.7, while explorer (Aug 14 session 2 reshuffle) falls back opencode-go/deepseek-v4-flash (paid Go-tier, uncapped) → zai-coding-plan/glm-4.7. See the YAML header in `agents/explorer.md`.

Only 2 agents have write access — by design, with different blast radii: **archivist** (scoped to vault paths: write ~/Main-vault/wiki/**, index.md, log.md, hotcache.md, .sisyphus/evidence/**, .sisyphus/plans/**, .sisyphus/boulder.json, .sisyphus/notepads/**, projects/**; deny: _.env_, _.pem, _.key, _credentials_, _secrets_, ~/Main-vault/raw/**) — vault mutations need containment. **fullstack-dev-tester** (edit: \*: allow) — this agent's job is to BUILD full-stack apps anywhere in a project workspace; broad edit access is required and intentional. Different agents, different blast radii — do not "tighten" fullstack-dev-tester to match archivist's scoped pattern. All other user-defined agents are read-only or read+network. oracle is the most restricted (read-only, no bash, no edit).

Temperature: 7 of 8 user-defined agents run at `temperature: 0.1`. **auditor is the deliberate exception at `temperature: 0.0`** — deterministic validation output, no creativity wanted. Do not "normalize" auditor to 0.1.

Permission schema consistency: all 8 user-defined `.md` files use the modern `permission:` block (no legacy `tools:`/`permissions:`). Minor stylistic variance — auditor/post-reviewer/reviewer use `edit: deny` shorthand while the other 5 use `edit: { "*": deny }` object form; both are valid and normalized by `migrateAgentConfig()` at load time. Plugin Layer 0 (trust-root-paths.js) additionally blocks ALL tools — including archivist and fullstack-dev-tester — from writing to ~/.sisyphus/state.json, workflow.yaml, verdict files, /proc, opencode.json (write-only; reads allowed via READ_EXCEPTION_PATTERNS), and plugin source. Trust-root protection is unconditional: no phase, no approval, no agent permission overrides it. Layer 3.7 (sandbox-policy.js) adds path-scoped command relaxation: when cwd is in a configured sandbox_paths prefix AND the bash command matches sandbox_allowed_commands, the gate allows without phase ceremony (opt-in via opencode.json). Layers 0–3.5 remain unconditional.
