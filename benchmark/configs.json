{
  "benchmark_name": "opencode-calibration-v1",
  "benchmark_version": "1.0.0",
  "configs": [
    {
      "id": "baseline",
      "description": "Current explore agent configuration — read + grep + websearch + webfetch. No semantic search, no semble.",
      "tools_available": ["read", "grep", "websearch", "webfetch"],
      "model": "openrouter/deepseek/deepseek-v4-flash",
      "variant": "low",
      "explore_agent_prompt": "Use grep for pattern matching, Read for file contents, websearch/webfetch for external research. No semantic search tools available."
    },
    {
      "id": "semble-first",
      "description": "Same model and tools but with semble_search/semble_find_related added as preferred first pass, grep/read as fallback.",
      "tools_available": ["semble_search", "semble_find_related", "read", "grep", "websearch", "webfetch"],
      "model": "openrouter/deepseek/deepseek-v4-flash",
      "variant": "low",
      "explore_agent_prompt": "Start with semble_search for semantic code search. Only fall back to grep/Read when semble can't find what you need or when you need exhaustive literal matching."
    }
  ]
}
